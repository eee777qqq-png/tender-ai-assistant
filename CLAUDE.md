# Тендерный ИИ-ассистент — контекст проекта

Этот файл читается автоматически в начале каждой сессии Claude Code. Здесь — суть проекта, чтобы не объяснять заново каждый раз.

## О продукте

Автоматизация подготовки к участию в госзакупках (44-ФЗ/223-ФЗ) для малого и среднего бизнеса (СМСП). Два сегмента: малому бизнесу — полный аутсорс под ключ, среднему — инструмент усиления штатного тендерного специалиста.

## Пилот

- **Ниша:** строительные работы / капремонт
- **Регионы:** Москва, Московская область, Краснодарский край, Ростовская область (без Крыма и Севастополя)
- **Владелец продукта:** Edwin — не разработчик, принимает бизнес-решения, лично выступает экспертом-проверяющим на пилоте (проверяет смету и документацию до момента, пока агенты не наберут статистику для перехода на выборочный аудит)
- **Юридическая форма:** сейчас ИП Edwin, только для собственного тестирования и 2-3 друзей. Полный переход на ООО (новые ЭЦП, перерегистрация в ЕИС, новые договоры, новый расчётный счёт, новый оператор персданных) — делается единым пакетом непосредственно перед открытием для внешних клиентов, не раньше и не по частям.

## ⚠️ Известные пробелы и нерешённые вопросы

**Обязательное правило для Claude Code: веди этот раздел активно.**
- Когда встречаешь заглушку, упрощение, несогласованное с владельцем правило, или открытый вопрос, требующий решения человека — добавляй сюда пункт: что это, где в коде (файл/функция), почему это временно.
- Когда вопрос решён (правило согласовано, заглушка заменена реальной логикой) — удаляй пункт из списка. Список должен отражать текущее состояние, не историю.
- В начале каждой сессии кратко проверяй этот раздел и, если уместно к текущей задаче, напоминай о нём.

### Открытые пункты

1. **Формула финансовой готовности в Агенте 2** (`src/classifier/matching.py`) — по-прежнему грубая заглушка «выручка ≥ НМЦК», не переключена на реальную маржу. Агент 5 (`src/profitability_estimator`) теперь считает НМЦК − себестоимость − обеспечение − налоги по паре клиент+закупка, но эта интеграция — отдельная нерешённая задача: использовать ли margin Агента 5 как более точный критерий финансовой готовности вместо грубой заглушки Агента 2 — не сделано.
2. **Источник нормативной базы для Агента 4 (сметчик) технически найден и подключён кодом, 2026-09-18.** У ФГИС ЦС есть открытый раздел данных, отдающий и полный ФСНБ-2022 (ГЭСН — 30 028 позиций работ, плюс федеральная база цен на материалы/машины/технику), и квартальные региональные индексы пересчёта цен по всем субъектам РФ, включая все 4 пилотных региона — обычной прямой HTTP-ссылкой/JSON-API, без логина, без ЕСИА, без платного крипто-клиента Jinn-Client. Подробности — `docs/agent4-ai-matching-feasibility.md`. Резервный вариант — ответ от Сметрикс по B2B/API всё ещё не пришёл, но, похоже, уже не обязателен. **Методологический вопрос по расчёту региональной цены был обнаружен и решён в тот же день, 2026-09-18 — см. подраздел «Решено» ниже.**

3. **Агент 6 (Сборщик документов)** (`src/document_assembler`) — технически подключён к Агенту 3 (`extracted_requirements` дополняет обязательность полей, скрытые риски прокидываются в `DocumentPackage.hidden_risks`), но содержательной генерации документов всё ещё нет — источником текста самих документов Агент 3 в его нынешнем виде (структурированное извлечение требований) не является, это отдельная нерешённая задача.
4. **Метод извлечения требований в Агенте 3** (`src/document_analyst/extractor.py`) — не выбран. Сейчас это эвристический поиск по ключевым словам/регулярным выражениям, подобранный под три придуманных тестовых документа (`sample_documents.py`), а не под реальную документацию закупок с произвольными формулировками. Нет решения, будет ли это LLM-экстракция, промышленный NLP-парсер или что-то ещё — именно поэтому 100% выдачи идёт через эксперта (протокол ниже), пока не накоплена статистика на реальных документах.
5. **Перевод скрытых рисков в понятный текст для собственника в Агенте 8** (`src/client_consultant/consultant.py`, `_RISK_PRACTICAL_MEANING`) — сейчас 5 фиксированных шаблонов, по одному на каждую категорию риска (`RiskCategory`), без учёта конкретной цитаты/цифр из конкретной находки — общий смысл категории, не интеллектуальный пересказ. Так же и подсказки «что сделать» для недостающих полей (`_ACTION_HINTS`) — фиксированный словарь по именам полей, которые сейчас строит Агент 6; новое поле или новая категория риска потребует вручную добавить текст, автоматического пересказа произвольного текста нет. Решения, нужен ли здесь LLM для более живого пересказа, не принято.
6. **Тексты Агента 12 (границы ответственности) не проверены юристом.** И `CLIENT_SUMMARY_LEGAL_NOTICE` (`src/legal_boundaries/notices.py`, вшито в вывод Агента 8) для интерфейса, и черновик формулировки для будущего договора-оферты (`docs/legal-boundary-draft.md`) — рабочие варианты владельца продукта, не формулировки, подготовленные или проверенные практикующим юристом. Использовать черновик из `docs/legal-boundary-draft.md` в реальном договоре или другом документе с юридической силой для внешних клиентов нельзя до юридической проверки — см. пометку в самом файле.
7. **Таблица «налоговый режим клиента → сумма налога в рублях» в Агенте 5** (`src/profitability_estimator/estimator.py`, `_TAX_RULES`) не проверена бухгалтером. Рабочее приближение Claude: НДС везде считается от полной НМЦК без вычета входного НДС (для «ОСН + НДС 22% (с вычетом)» это неточно — вычитать не на чем, данных о затратах с НДС нет), налог на прибыль ОСН зафиксирован на стандартных 20% (не хранится в `TaxRegimeChoice`, клиенту в форме показана только ставка НДС), страховые взносы и прочие обязательные платежи не учтены. До проверки бухгалтером/владельцем — маржа Агента 5 показательная, не окончательная цифра для клиента.
8. **Оценка вероятности победы (изначально — сам Агент 5 в таблице ниже) отделена от оценки выгоды и не специфицирована.** Агент 5 сейчас считает только маржу и качественные флаги риска без цифр (`src/profitability_estimator`) — своей статистики побед/поражений ещё нет (cold start), вычислять число из непроверенных допущений решили не делать. Переход на числовую вероятность станет возможен только после того, как Агент 9 (клиентская аналитика, тоже не специфицирован) накопит статистику — как именно это будет работать, не спроектировано.
9. **Трудозатраты машинистов — логика реализована в коде 2026-09-18, но пункт формально НЕ закрыт до письменного подтверждения от Smetrix (ожидается в начале следующей недели).** (Трудозатраты рабочих закрыты — см. подраздел «Решено».) `pricing.py`/`fsnb_parser.py`/`models.py`: новый `MachineLabourInfo` (`parse_fsbc_machine_labour_xml`) хранит `LabourMach`/`DriverCode` по каждому машинному ресурсу; `price_candidate_for_region()` теперь принимает необязательный `machine_labour` и, если для ресурса `labour_mach > 0`, прибавляет `labour_mach × текущая_ставка(driver_code)` (из того же `current_prices`/`RimWorkerSalaryRegistry`, что и для рабочих) к его цене; если ставка нужна, но не нашлась — ресурс честно `unresolved`, не тихая недооценка. `labour_mach == 0` (обычно нет `driver_code` — самоходное/электрическое оборудование без отдельного оператора) — ничего не прибавляется, машина без оператора. `ResourcePriceResolution.machinist_wage_added` — сколько из цены пришлось на эту добавку, для прозрачности перед экспертом. Обратная совместимость: без `machine_labour` (не передан) — поведение как раньше, добавки нет, ни один существующий тест/вызов не сломан. Реализовано на основании устного подтверждения Smetrix (см. ниже) — не финально. Покрыто юнит-тестами (`test_smeta_estimator_pricing.py`) и вживую на реальных данных Москвы в обоих e2e-тестах.

**Источник подтверждения — устный, не письменный.** На звонке со Smetrix (2026-09-18) практик подтвердил гипотезу Claude про `LabourMach`: это не единая формула на все машины, а свойство конкретной позиции — «для одной техники машинист уже в цене, для другой нет, нужно смотреть по факту записи» (дословно близко к сказанному), что соответствует наблюдаемому паттерну в данных (`LabourMach=1.00` → добавить отдельно, `0` → уже включено/не нужно). **Письменного подтверждения от Smetrix пока нет — ожидается в начале следующей недели.** До этого момента пункт остаётся открытым: если письменный ответ разойдётся с реализованной логикой, `pricing.py`/`fsnb_parser.py`/`models.py` (`MachineLabourInfo`) потребуют пересмотра — это явно помечено в докстрингах всех изменённых мест.

**Независимая проверка по официальным источникам, 2026-09-18 (не решила вопрос до конца, код не менялся):**
- **Исправлена неверная ссылка**: приказ Минстроя №326/пр (05.06.2019), упомянутый раньше как возможный источник методики — это «Методика расчёта ИНДЕКСОВ изменения сметной стоимости строительства», не про формулу оплаты труда машинистов вообще. Реально применимый и действующий документ — приказ №421/пр от 04.08.2020 «Методика определения сметной стоимости строительства...», раздел IV, п.79–83, формула (1.1) — введена/переписана приказом №557/пр от 07.07.2022, актуальность подтверждена: последующие правки (приказ №30/пр от 23.01.2025, приказ №42/пр от 30.01.2026, действует с 14.02.2026) пп.79–82 не затрагивают.
- **Подтверждено официально (несколько независимых источников):** оплата труда машинистов (ОТм) считается ОТДЕЛЬНО от стоимости эксплуатации машины (ЭММ), не входит в неё по умолчанию — формула (1.1) в 421/пр: ОТм = ΣΣ ЗТki×СЦk×Vi, где ЗТki — трудозатраты машиниста по машине k на измеритель сметной нормы (чел.-ч), СЦk — сметная цена его труда в текущем уровне цен (руб./чел.-ч, «на основании данных ФГИС ЦС» — то же значение, что уже подтверждено для рабочих через `RimWorkerSalaryRegistry`). Отдельно приказ №916/пр от 13.12.2021 «Методика определения сметных цен на эксплуатацию машин и механизмов» (в силе, per Минюст №68062, свежее ФСНБ-2022) двумя независимыми пересказами (base.garant.ru, sudact.ru) подтверждает то же самое другими словами: формула цены машино-часа (амортизация+ремонт+топливо+...) оплату труда машиниста не включает, она считается отдельным разделом методики. Отраслевая статья (i-tat.ru) независимо подтверждает: «ФОТ = ОТ + ОТм» — и описывает реальную путаницу у сметных программ с этим на практике, то есть вопрос объективно неочевиден, не только для этого проекта.
- **Всё ещё НЕ подтверждено ни одним источником:** что именно поле `LabourMach` в XML ФГИС ЦС дословно равно ЗТ (трудозатратам машиниста на машино-час) из формулы (1.1) — это остаётся правдоподобной, но не процитированной нигде реконструкцией (по паттерну в данных: `LabourMach=1.00`, когда есть `DriverCode`/`MachinistCategory`, и `0`, когда их нет). Официальных описаний конкретно этого XML/API-поля ФГИС ЦС в открытом доступе не нашлось — попытки достать разъяснения от сметных программ (ГРАНД-Смета, Турбосметчик), которые по идее должны были решить эту же задачу для тех же данных ФГИС ЦС, не удались технически (страницы на JS, не отдают текст без браузера).
- **Отдельно подтверждено вживую, 2026-09-18: «машинист» — единый обобщающий термин, отдельной категории «водитель» в данных нет.** Живой запрос `fetch_worker_salary_registry()` за Москву (не фикстура — реальные 186 позиций реестра на 3 квартал 2026) показал, что все 91 позиции с кодом `4-100-XXX` названы дословно одинаково — `"Средний разряд машинистов"` + номер разряда, различаются только разрядом (1,0–10,0+), не видом техники и не профессией. Слова «водитель» нет ни в одной из 186 записей реестра вообще. Это подтверждает, что `DriverCode` в ФСБЦ_Маш.xml (английское название поля, само по себе двусмысленное) для любой техники — включая грузовой автомобиль `91.14.02-001 "Автомобили бортовые"`, которым в быту скорее управляет «водитель» — указывает именно в общий пул «машинистов» по разряду, а не в отдельную категорию водителей. Другие категории того же реестра для контраста: `1-100`/`2-100` — рабочие, `3-100..3-400` — техники/инженеры, `5-100` — исполнители реставрационных работ.
- **Новый вопрос, всплывший в ходе проверки, не было в CLAUDE.md раньше:** та же неопределённость может касаться не только пути «базовая цена × индекс ГОСР», но и пути «текущая цена напрямую» (`current_prices`, приоритет 1 в `pricing.py`) — включает ли `estimatedPrice` из `/BuildingResources/Search/Machines` оплату труда машиниста уже внутри, или тоже нужно доплюсовывать `LabourMach × ставка`. Не проверено ни в одну сторону.
- **Вывод: методологический вопрос не стал однозначным**, хотя `pricing.py`/`fsnb_parser.py`/`models.py` уже реализуют `LabourMach`-логику (см. выше) — реализация остаётся не финальной до письменного ответа Smetrix. Ждём встречи с практикующим сметчиком. Стоит спросить его отдельно и про `LabourMach`, и про то, включает ли `estimatedPrice` зарплату машиниста; также у Edwin может быть доступ к разделу «База знаний» ФГИС ЦС (`fgiscs.minstroyrf.ru/#/Knowledges`) с более точным описанием полей API, если найдётся время проверить напрямую.
10. **Часть материалов ГЭСН задана категорией, не конкретным продуктом** (`<AbstractResource>` в XML, `abstract_resource_codes` в `GesnWorkItem`) — например «Материалы рулонные кровельные для нижних слоев» без указания, что конкретно использовать. Это не пробел в данных, который можно закрыть поиском источника — **это продуктовое решение владельца**: автоматически подставлять какой-то конкретный продукт категории (самый дешёвый? самый популярный по объёму продаж?) или оставлять выбор эксперту в каждом случае вручную. Пока не решено — остаётся `unresolved`, эксперт видит категорию и выбирает сам.
11. **`DEFAULT_ARCHIVE_URL` (`fsnb_client.py`) и `CURRENT_PERIOD_ID` (`regional_pricing_client.py`) — захардкожены на конкретную версию/квартал, не отслеживаются автоматически.** Перепроверено 2026-09-18 вечером — 3 квартал 2026 всё ещё актуален, новых версий не появилось. Осознанно не стал делать автообновление сейчас (это отдельная инженерная задача — завести то же отслеживание версий через Агента 10, что уже есть для самих индексов ГОСР, — не вопрос недостающей информации), обновлять вручную по мере выхода новых версий.
12. **Агент 4 подключён к Агенту 5 (`src/smeta_estimator/cost_estimate.py`, 2026-09-18), но объём работ по смете конкретной закупки — не автоматизированный внешний вход, а ручной параметр (`SmetaLineItem.work_volume`).** Это единственное место в этой связке, где стык получился не таким гладким, как между Агентом 3 и Агентом 6: там Агент 3 сам извлекает из текста документации всё, что нужно Агенту 6. Здесь же ни Агент 3 (извлекает сроки/обеспечение/риски, не ведомость объёмов работ), ни Агент 4 (не реализовано — см. таблицу ниже) не считают, сколько именно "100 м2" кровли или "м3" бетона нужно по конкретной закупке — эту цифру по-прежнему подставляет человек. `build_cost_estimate()` требует `MatchResult.expert_reviewed=True` для каждой позиции (тот же гейт, что у Агента 3 в `assemble_document_package()`/`estimate_profitability()`) и передаёт себестоимость в `CostEstimate` с новыми полями `expert_reviewed`/`is_complete` — `estimate_profitability()` отказывает, если `expert_reviewed=False`, и добавляет риск-флаг, если `is_complete=False` (часть позиций сметы не оценена или оценена частично), не блокируя расчёт маржи целиком.
13. **Переходник `notice_document_to_tender()` закрывает 7 из 10 полей `Tender` из извещения — 3 оставшихся (`okpd2_code`, `region_code`, `publish_date`) в извещении не искали, не подтверждены.** `requires_sro`/`min_experience_years`/`submission_deadline` (которых нет в контракте структурно) плюс `name`/`customer_name`/`max_price`/`purchase_number` — закрыты. `document_to_tender()` (контракты) оставлен в коде как есть, не удалён — просто больше не путь по умолчанию (см. «Решено» про переключение продакшен-источника). **Оставшийся пробел:** `okpd2_code` — есть ли свой ОКПД2 в извещении, каким путём — не проверяли; `region_code`/`publish_date` — вероятно, тоже где-то в извещении есть (например, `commonInfo` может содержать дату публикации, у ответственной организации обычно есть код региона), но их пути не искали и не запрашивали у Edwin в этот раз. Явные обязательные параметры `notice_document_to_tender()`, заглушка не подставляется.
14. **Находка 2026-09-23, НЕ реализовано: извещение содержит структурированные суммы обеспечения (`applicationGuarantee/amount`, `provisionWarranty/amount`) — потенциально закрывает то же самое, что должен был дать платный источник МультиТендер.** Замечено в том же реальном документе-извещении (`0373200298826000007`), где нашли остальные поля — в отличие от `requires_sro`/`min_experience_years` (текст, разбирается регуляркой), это отдельные числовые поля: сумма обеспечения ЗАЯВКИ (`applicationGuarantee/amount`) и сумма гарантии исполнения КОНТРАКТА (`provisionWarranty/amount`). Пути НЕ проверены на извлечение (только замечены в структуре), суффиксы в `eis_client/notice_parser.py` намеренно не заведены — это отдельная задача, не сделанная в рамках этой правки. Продуктовое значение: Агент 5 (`profitability_estimator`) сейчас считает стоимость обеспечения приближённо (3% годовых от суммы), а не берёт готовую сумму из документа закупки — если поле надёжно извлекается, точность оценки выгоды может вырасти без платных источников. **Следующий шаг:** когда дойдёт очередь — извлечь структурно (тем же принципом пути, что остальные поля извещения), проверить на реальном документе, свести с логикой Агента 5.
15. **Риск переключения продакшена на извещения, 2026-09-23 — не проверено: находит ли `_find_okpd2_codes()` ОКПД2 в извещениях вообще, тем же структурным путём, что в контрактах.** Путь `KTRU/OKPD2/code` подтверждён только на реальном КОНТРАКТЕ (`contract_2770206615726000446`) — в извещении (`0373200298826000007`) структуру ОКПД2 не искали, значит с 2026-09-23, когда `run_monitor.py` по умолчанию читает извещения (см. «Решено»), классификатор Агента 1 (`EISClient.get_construction_documents` → `_find_okpd2_codes`) опирается на путь, ПОДТВЕРЖДЁННЫЙ ДЛЯ ДРУГОГО ТИПА ДОКУМЕНТА. Если в извещении структура другая — основной способ не найдёт ничего, останется только резервная эвристика по имени тега (тоже не подтверждена), и монитор может вернуться к тем же ложным нулям, что уже были с эхо-ответом/403/старой ОКПД2-эвристикой в этой же сессии диагностики (см. историю в «Решено»). **Не додумано и не исправлено вслепую** — по тому же принципу, что и раньше: путь меняется только по реальному документу, не по аналогии. **Следующий шаг:** первый же реальный прогон `run_monitor.py`/`fetch_purchases.py` под новым источником покажет число в логе («XML-документов в архивах — N, с найденным ОКПД2 — M»); если M заметно меньше N при том, что закупки по стройке точно есть — тот же `inspect_eis_xml.py --list` на архиве извещений покажет, где реально лежит ОКПД2 в этом типе документа.

### Решено

Пункты ниже больше не открытые вопросы — оставлены для истории решений, не как активная работа. Нумерация внутри этого подраздела не связана с нумерацией открытых пунктов выше.

- **Продакшен-поток Агента 1 переключён с реестра КОНТРАКТОВ на реестр ИЗВЕЩЕНИЙ — решение принято и реализовано, 2026-09-23.** Закупки, в которых клиент реально может участвовать, это предстоящие извещения, не архив уже заключённых сделок — прежний источник (`RGK`/`contract`) для цели проекта был структурно не тем. **Изменено в коде:** `EISConfig.from_env()` (`src/eis_client/config.py`) — значения по умолчанию для `EIS_SUBSYSTEM_TYPE`/`EIS_DOCUMENT_TYPE44` теперь `PRIZ`/`epNotificationEF2020`, не `RGK`/`contract`; `.env.example` и README обновлены тем же. `document_to_tender()` (контракты) в коде остался, не удалён — реестр контрактов по-прежнему поддержан на случай, если понадобится, просто больше не путь по умолчанию. **`data/monitor.sqlite3` защищена от молчаливого смешивания источников, не только предупреждением в CLAUDE.md.** `MonitorStore` теперь помнит `source_signature` (`EIS_SUBSYSTEM_TYPE:EIS_DOCUMENT_TYPE44`) для каждой обработанной даты — дата, обработанная под старым источником (контракты), для нового источника (извещения) НЕ считается обработанной вообще, не «устаревшие индексы ОКПД2», а данные другого типа документа целиком: другая структура, другой набор полей, несопоставимо. Существующая база (если создана до 2026-09-23) при первом открытии автоматически мигрирует — старые строки помечаются `RGK:contract` (буквально то, что тогда читалось, не предположение), новая схема требует подписи источника у каждой записи. `run_monitor.py` при запуске явно предупреждает в логе, если в хранилище есть даты под другой подписью, и называет их число — но не перезапрашивает их автоматически (это могло бы быть много сетевых запросов сразу) — решение, насколько глубоко перезапросить историю под новым источником, за Edwin (`--start-date`). **Не сделано и не должно быть спутано с этим решением:** классификатор ОКПД2 (`_find_okpd2_codes`) не проверен на извещениях (см. открытый п.15) — переключение источника подтверждено на уровне «сервис отдаёт документ», не «наш парсер гарантированно находит в нём ОКПД2».

- **Код реестра извещений для Агента 1 — гипотеза `PRIZ`+`epNotificationEF2020` подтверждена реальным запросом, 2026-09-23.** Точного значения в открытой инструкции не было (Приложение 1 отсылает к закрытому Альбому ТФФ) — по косвенным намёкам (формат номера в примерах `getDocsByReestrNumberRequest`, PRIZ-архив со скриншота раздела 7) выдвинута гипотеза `EIS_SUBSYSTEM_TYPE=PRIZ` + `EIS_DOCUMENT_TYPE44=epNotificationEF2020` (электронный аукцион). **Edwin проверил реальным запросом** (переопределение переменных окружения в терминале, не правка `.env`) — сервис вернул настоящий строительный документ-извещение (`0373200298826000007`), не SOAP-фолт. Гипотеза подтверждена без единой попытки перебора — `epNotificationEF2020` сработал с первого раза. **Обновление 2026-09-23, позже в тот же день:** переключение сделано — рабочий поток Агента 1 (`run_monitor.py`/`EISConfig.from_env()` по умолчанию) теперь читает реестр извещений, не контрактов; подробности самого переключения и защиты `data/monitor.sqlite3` от смешивания источников — отдельная запись выше «Продакшен-поток Агента 1 переключён...». Подробности структуры извещения и что из неё теперь извлекается — см. отдельную запись ниже.
- **Реестр КОНТРАКТОВ структурно не может дать `requires_sro`/`min_experience_years`/`submission_deadline` — закрыто переходом на извещение как источник этих полей, не улучшением парсинга контракта, 2026-09-23.** Edwin прислал структуру реального извещения (`0373200298826000007`, стройка, тот же документ, что подтвердил гипотезу `PRIZ` выше): `commonInfo/purchaseNumber` (номер закупки), `purchaseObjectsInfo/notDrugPurchaseObjectsInfo/purchaseObject/name` (наименование), `purchaseResponsibleInfo/responsibleOrgInfo/fullName` (заказчик), `notificationInfo/contractConditionsInfo/maxPriceInfo/maxPrice` (НМЦК), `notificationInfo/procedureInfo/collectingInfo/endDT` (срок подачи заявок) — все структурные, тот же принцип пути, что у ОКПД2/реестрового номера. `requires_sro`/`min_experience_years` — не отдельные поля, а текст внутри `notificationInfo/requirementsInfo/requirementInfo/addRequirements/addRequirement/content` (до нескольких предложений) — разобран не новой эвристикой, а переиспользованием регулярок Агента 3 (`document_analyst.extractor.mentions_sro_requirement`/`extract_min_experience_years`, вынесены в публичные функции специально для этого переиспользования). **Реализовано:** новый модуль `src/eis_client/notice_parser.py` (`extract_notice_fields()`) + `eis_client.tender_adapter.notice_document_to_tender()` — отдельная функция от `document_to_tender()` (контракты), не расширение той же, потому что извещение и контракт — совсем разные документы. Покрыто тестами на структуре реального документа, включая честные дефолты (`requires_sro=False`/`min_experience_years=0`), если требование не упомянуто текстом, и явный отказ, если не нашлись `purchase_number`/`name`/`customer_name`/`max_price`/`submission_deadline` (`tests/test_eis_notice_parser.py`). **Не закрыто этим же документом:** `okpd2_code`, `region_code`, `publish_date` — не подтверждены (не искали) в извещении, по-прежнему обязательные параметры `notice_document_to_tender()`, см. открытый п.13.

- **Вся цепочка «получить настоящие документы закупок из ЕИС» — закрыта и подтверждена реальными данными, 2026-09-23.** Три независимые поломки шли одна за другой на одном и том же запросе (`python src/fetch_purchases.py --date 2026-09-21`, реестр контрактов, регион 77) — каждая маскировала следующую, пока не была исправлена: (1) неверный namespace в SOAP-конверте → сервис отвечал эхом запроса вместо ответа (EIS-891228); (2) GET за архивом без токена → 403 Forbidden; (3) поиск ОКПД2 по имени тега вместо реальной структуры документа → 0 найденных кодов из 1640 документов. Подробности каждой поломки и исправления — в отдельных записях ниже. **Итоговое подтверждение на реальном прогоне Edwin:** из 1640 XML-документов 674 содержат распознанный код ОКПД2, из них 8 — реальные строительные контракты за 2026-09-21 в Москве, с настоящими кодами (`41.20.40`, `43.22.12`, `43.29` и другими) и рабочими ссылками на документы. Монитор (Агент 1) на реальных данных ЕИС работает от SOAP-запроса до отбора закупок по стройке — это больше не каркас, это рабочая цепочка. **Осталось непроверенным/структурно недоступным** (см. открытые пункты 3 и 14 выше): точный код реестра ИЗВЕЩЕНИЙ вместо контрактов (`PRIZ`, гипотеза) и три поля `Tender` (`requires_sro`, `min_experience_years`, `submission_deadline`), которых в реестре контрактов нет вообще — реестровый номер (`_find_reestr_number()`) с 2026-09-23 уже подтверждён и исправлен, см. отдельную запись ниже. **Важное предупреждение, не разовое:** даты, которые `run_monitor.py` пометил обработанными в `data/monitor.sqlite3`, пока сервис не работал (минимум с 2026-09-17 по 2026-09-23 — эхо, затем 403, затем нулевой ОКПД2-поиск), несут ЛОЖНЫЙ ноль документов, не честный «в этот день стройки не было». Перед тем как ставить `run_monitor.py` на регулярное расписание, эти даты нужно перезапросить вручную — опустошить `data/monitor.sqlite3` (или удалить конкретные строки) и прогнать `run_monitor.py --start-date` заново с 2026-09-17.

- **Реестровый номер закупки (`_find_reestr_number()`) — подтверждён на реальном документе и исправлен, 2026-09-23.** На том же реальном контракте, где нашли структуру ОКПД2 (`contract_2770206615726000446`, стройка), Edwin нашёл путь до номера: `.../foundation/fcsOrder/order/notificationNumber` (19 цифр) — это номер ИЗВЕЩЕНИЯ, на основании которого заключён контракт, не номер самого контракта. Прежняя эвристика по имени тега (`reestrnum`) не находила это поле — «reestrnum» не входит в имя `notificationNumber`. **Исправлено в `_find_reestr_number()` (`src/eis_client/client.py`):** тот же принцип, что и у ОКПД2 (см. ниже) — совпадение по последним 4 звеньям пути тега `foundation/fcsOrder/order/notificationNumber`, без учёта регистра/namespace. Прежняя эвристика по имени тега оставлена резервным способом. Покрыто тестами на структуре реального документа (`tests/test_client.py`). `tender_adapter.py` обновлён: `_find_reestr_number()`/`Tender.purchase_number` подтверждены, документация модуля переписана с честным разбором остальных 8 полей — см. открытый п.13.
- **403 Forbidden при скачивании архива ЕИС — решено 2026-09-23, проверено реальным запросом Edwin:** за 2026-09-21 скачано 17 архивов, внутри 1640 XML-документов — после добавления HTTP-заголовка `individualPerson_token` к GET-запросу (`EISClient._download_auth_headers()`), найденного на скриншотах Postman в разделе 7 инструкции. Дальше — диагностика до исправления, для контекста. Реальный запрос Edwin (2026-09-23, `python src/fetch_purchases.py --date 2026-09-21`, настоящий токен физлица) — SOAP-шаг проходит, ответ настоящий, ссылка на архив приходит. Но GET по этой ссылке (`https://int.zakupki.gov.ru/dstore/common/download/compound?docRequestUid=...&compoundUid=...`) возвращает **403 Forbidden**. **Диагноз (2026-09-23, по инструкции, без нового запроса к серверу):** наш `EISClient.download_archive()` отправлял GET вообще без токена — только `Content-Type`. В тексте инструкции (раздел 7 «Скачивание архивов с информацией», стр. 37, плюс абзацы «Для скачивания документов... направить GET-запрос» в разделах 4.1/5.1/6.1) про авторизацию GET-запроса не сказано ни слова, но **на всех трёх скриншотах Postman в разделе 7 (стр. 37–39, вкладка Headers) у GET-запроса стоит HTTP-заголовок `individualPerson_token: <токен>`** — то же имя, что у поля в SOAP-заголовке, не `Authorization`, и не в теле, и сервер отвечает `200 OK`. Косвенно это подтверждает и раздел 6.1 про ВСРЗ: «В GET-запросе не обязательно указывать токен ВСРЗ» — то есть токен в GET-запросе в принципе предусмотрен. **Исправлено в коде 2026-09-23, реальным запросом ЕЩЁ НЕ ПРОВЕРЕНО:** `EISClient._download_auth_headers()` (`src/eis_client/client.py`) добавляет заголовок `individualPerson_token` к GET за архивом для `individual_person`; для `legal_entity` ничего не добавляется (там mTLS-сертификат сессии). **Оговорки, почему это может не сработать:** (а) скриншоты сняты на тестовом стенде (`int44.eis3.lanit.ru`) со ссылками старого вида `/dstore/ip/download/<подсистема>/file.zip?ticket=...`, где разовый `ticket` встроен прямо в URL, а у нас реальная ссылка другого вида — `/dstore/common/download/compound?docRequestUid=...&compoundUid=...`, без `ticket`; такой формат в инструкции не описан вообще, так что гарантии, что для него работает тот же заголовок, нет; (б) если заголовок не поможет, следующие гипотезы — архив по `docRequestUid` формируется асинхронно и ещё не готов в момент скачивания (тогда нужен повтор через паузу) или ссылка одноразовая/с коротким сроком жизни; обе не проверены. **Обновление 2026-09-23:** повторные реальные запросы Edwin за 2026-09-21 и 2026-09-15 прошли без ошибок с результатом «Найдено: 0». Для 2026-09-21 ранее приходила ссылка на архив (и 403) — раз ошибки больше нет, скачивание, вероятно, прошло, но это не доказано: «0» не отличает «архивов не было» от «архивы скачаны, стройки нет». С 2026-09-23 `get_construction_documents()` пишет в лог число архивов, XML-документов, документов с найденным ОКПД2 и примеры кодов — следующий запуск это покажет; если архивов ≥1 — 403 закрыт, пункт переносится в «Решено». Причины самих нулей на реестре контрактов — теперь понятны: код реестра извещений тоже нашёлся и подтверждён (`PRIZ`, см. отдельную запись ниже), реестр контрактов просто содержит меньше стройки в принципе; ОКПД2-эвристика на реальных документах проверена и исправлена, см. отдельную запись ниже. Подтверждено 2026-09-23: 17 архивов и 1640 XML-документов скачались без единой ошибки — 403 закрыт.
- **Эхо-ответ ЕИС вместо `getDocsByOrgRegionResponse` — решено 2026-09-23, источник: техподдержка ЕИС, обращение EIS-891228.** Причина — неверный namespace `xmlns:ws` в SOAP-конверте: для физлиц нужен `http://zakupki.gov.ru/fz44/get-docs-ip/ws`, а код (следуя примеру из раздела 5.3 инструкции) подставлял туда физический URL сервиса. Исправлено в `src/eis_client/config.py`: `ENDPOINT_URLS` («куда стучимся» — адрес HTTP POST, не менялся) и `ENVELOPE_NAMESPACES` («что написано в конверте») теперь разные словари, свойства `EISConfig.endpoint_url` / `EISConfig.envelope_namespace`. **Проверено реальным запросом Edwin** (`python src/fetch_purchases.py --date 2026-09-21`, настоящий токен): эха больше нет, SOAP-ответ содержит настоящие `docRequestUid`/`compoundUid` и ссылку на архив. Для `legal_entity` namespace НЕ менялся — техподдержка прислала значение только для физлиц, уточнить при переходе на ООО. Дальше — история диагностики до исправления, для контекста. Первый запрос (2026-09-17, регион 77) прошёл TLS/сеть/токен в заголовке без ошибок (HTTP 200), но сервис вернул буквальное эхо отправленного запроса вместо `getDocsByOrgRegionResponse` с `archiveUrl` — ни SOAP-фолт, ни сетевая ошибка. Перепробовано без результата: варианты `SOAPAction`, `User-Agent`, повторный запрос с cookie-привязкой балансировщика (`priv-dstorebus-route`). **Повторная проверка в тот же день (2026-09-17, тот же запрос, без новых технических вариантов):** первая попытка не достучалась до сервиса вовсе (сетевой таймаут на подключении, вероятно случайная сетевая помеха, не воспроизведена повторно), вторая попытка прошла TLS/сеть/токен так же без ошибок (HTTP 200) и вернула тот же самый результат — буквальное эхо запроса, без `archiveUrl`. Изменений нет. Похоже на то, что доступ по токену ещё не полностью активирован на стороне ЕИС — не подтверждено. **Третья проверка (2026-09-18):** колл-центр ЕИС объяснил, что регистрация синхронизируется в базу только после полуночи следующих суток — проверено на дату 2026-09-17 (реальный `python src/run_monitor.py`, не только сырой запрос): результат не изменился, снова буквальное эхо, `archiveUrl` нет. Объяснение колл-центра не подтвердилось на практике. **Проверена и отклонена гипотеза «неверный endpoint/namespace» (по тексту инструкции; для namespace вывод оказался неверным — см. начало этого пункта):** запрос уходит на `https://int.zakupki.gov.ru/eis-integration/services/getDocsIP` (не на `getDocsLE` для юрлиц — `EIS_CONSUMER_TYPE=individual_person` работает как задумано), и namespace `xmlns:ws` в самом XML-конверте — та же самая строка адреса `getDocsIP` (`src/eis_client/soap_request.py`), что дословно совпадает с примером в разделе 5.3 официальной инструкции (`docs/eis-integration-instruction-2025.pdf`) для физлиц. Отдельного «содержательного» namespace вида `http://zakupki.gov.ru/fz44/get-docs-le/ws` (упомянутого при проверке как гипотеза) в этом документе нет вообще — ни для LE, ни для IP: в обоих разделах инструкции (4.3 — LE, 5.3 — IP) namespace всегда равен физическому URL сервиса. Если у Edwin есть другой источник (WSDL или другая версия инструкции) с таким namespace — стоит прислать, чтобы свериться; без него менять код на основании этой гипотезы не на что опереться. Подробности — README.md, раздел «Что сервис умеет и чего не умеет». **Проверена и отклонена гипотеза «устаревший домен» (2026-09-21, без нового запроса к серверу — только сверка конфигурации с текстом инструкции):** `config.py` для `individual_person` уже использует `int.zakupki.gov.ru` (не старый `int44.zakupki.gov.ru`) — это ровно тот домен, что во ВСЕХ примерах раздела 5 инструкции для физлиц, без единого исключения. Домен для нашего реального (эхо-)запроса — актуальный, менять нечего. **Побочная находка, не относится к нашей проблеме (у нас `individual_person`, не `legal_entity`):** сама инструкция внутренне противоречива по юрлицам — SOAP-эндпоинт/namespace для LE везде `int44-ttls-cert.zakupki.gov.ru`, но пример настройки `stunnel` для ГОСТ-сертификатов (Приложения 3–4) указывает `connect = int44.zakupki.gov.ru:443` — **другой домен, без `-ttls-cert`**. Не проверено и не обязательно ошибка (это может быть штатная разница между хостом для TLS/SNI-маршрутизации и хостом для самого TCP-подключения через туннель) — но если/когда дело дойдёт до `legal_entity` (переход на ООО), эту деталь нужно будет прояснить отдельно, не предполагать, что оба места инструкции говорят про один и тот же адрес.
- **Поиск ОКПД2 не находил ни одного кода ни в одном реальном документе ЕИС — причина найдена и исправлена, подтверждено на реальных данных, 2026-09-23.** Первый рабочий SOAP-ответ с настоящими архивами (после исправления namespace и 403 выше) дал 17 архивов, 1640 XML-документов — но `_find_okpd2_codes()` (эвристика по имени тега, похожему на «okpd») не нашла ни одного кода нигде, не только по стройке. Edwin прислал структуру одного реального документа через `src/inspect_eis_xml.py` (скелет тегов без содержимого заказчика): код лежит по пути `export/contract/products/product/KTRU/OKPD2/code` (пример значения: `32.50.21.150`) — сам тег называется просто `<code>`, «okpd» есть только в имени тега-**предка** через один уровень (`<OKPD2>`), обёрнутого в `<KTRU>`. Старая эвристика проверяла только собственное имя тега — `<OKPD2>` не содержит текста сама, текст лежит в дочернем `<code>`, поэтому не находила ничего. **Исправлено в `_find_okpd2_codes()` (`src/eis_client/client.py`):** основной способ — совпадение по последним трём звеньям пути тега (без учёта регистра/namespace) `.../KTRU/OKPD2/code`, обход всего дерева документа (все `products/product`, если позиций несколько, не только первая). Прежняя эвристика по имени тега оставлена вторым, резервным способом на случай других типов документов (извещений), структуру которых ещё не видели. **Подтверждено на реальном прогоне Edwin (не на придуманном примере):** из 1640 XML-документов 674 нашли код ОКПД2, 8 из них прошли фильтр по стройке (раздел F, коды `41.20.40`, `43.22.12`, `43.29` и другие), со ссылками на реальные документы. Покрыто тестами на структуре реального документа (`tests/test_client.py`), включая несколько позиций закупки и дедупликацию.
- **Методологическая ошибка в расчёте региональной цены Агента 4 — найдена, диагностирована и исправлена, 2026-09-18.** Причина была ровно та, что и предполагалась: `index_parser.py` (удалён) парсил письма Минстроя с **общими индексами «к ФЕР-2001/ТЕР-2001»** — это индексы для другого, более старого базисно-индексного метода (база — цены 2001 года), а ФСНБ-2022 — ресурсно-индексный метод с базой **01.01.2022** (подтверждено официальным разъяснением Минстроя, приказ №1046/пр — тот же, которым утверждена сама ФСНБ-2022). Прямое умножение текущей `base_price` на индекс не того метода задваивало пересчёт. Исправление: найден и подключён правильный источник — «Индексы по группам однородных строительных ресурсов (ГОСР)» на `fgiscs.minstroyrf.ru/prices` (`src/smeta_estimator/regional_pricing_client.py` + `regional_pricing_parser.py`), масштаб значений (~1.2–2.7) на порядок отличается от прежних (~14–60), проверено на реальных данных Москвы. Плюс реализован приоритет из ТЗ: сначала текущая цена ресурса напрямую (если опубликована), иначе базисная цена × ГОСР-индекс для группы именно этого ресурса — не единый общий множитель на всю позицию (`pricing.py`, полностью переписан). Остаточные вопросы по Агенту 4 — часть тоже решена (трудозатраты рабочих, охват ГОСР по машинам, пагинация — см. рядом в этом же подразделе), часть ещё открыта (трудозатраты машинистов, категории ГЭСН без выбранного продукта, захардкоженные версии источников — открытые пункты 10–12 выше).
- **Трудозатраты рабочих — закрыто, 2026-09-18 вечером.** Найден и подключён `/api/EstimatedPrice/RimWorkerSalaryRegistry` (`regional_pricing_client.fetch_worker_salary_registry` + `regional_pricing_parser.parse_worker_salary_registry`) — готовая опубликованная сметная цена, руб./чел.-ч, по коду разряда рабочего (`1-100-XX`, тот же код, что `Resource Code` в ГЭСН); объединяется с `current_prices` в `pricing.py` как есть, отдельный параметр не понадобился. Проверено на реальных данных Москвы (код `1-100-38` → 723,54 руб./чел.-ч). Тот же реестр содержит и коды вида `4-100-XXX` (совпадают с `DriverCode` в ФСБЦ_Маш.xml — подтверждено вживую: `4-100-060` → 994,18 руб./чел.-ч) — эти коды используются трудозатратами машинистов, см. открытый п.9 выше.
- **Покрытие ГОСР по машинам неполное — закрыто комбинированием с текущими ценами, 2026-09-18 вечером.** На реальном примере (Москва, 3 квартал 2026) из 5 машинных кодов тестовой позиции индекс ГОСР нашёлся только для 2 — но проверка показала, что для остальных 3 в этот же квартал была опубликована текущая цена напрямую (`/BuildingResources/Search/Machines`). Вместе источники покрыли все 5 — не совпадение, а ожидаемая логика: ФГИС ЦС публикует для ресурса либо текущую цену, либо ГОСР-индекс, редко оба сразу. Остаточный риск: для реального полного каталога (30 028 позиций) не проверено, есть ли коды без обоих источников сразу — они по-прежнему честно останутся `unresolved`, не тихим нулём, если такое случится.
- **Пагинация API текущих цен — проверена и защищена, 2026-09-18 вечером, не просто отмечена как риск.** Подтверждено вживую: у `RimWorkerSalaryRegistry` `take` реально ограничивает выдачу (Москва: `total=186`, `take=100` вернул только 100 — без догрузки часть ставок терялась бы молча); `fetch_worker_salary_registry` теперь честно догружает страницы до `total`. У `BuildingResources/Search/*` на практике `take` не ограничивал выдачу (Москва: ~2700 материалов пришли одним ответом), но `fetch_current_prices_json` всё равно не доверяет этому слепо — сверяет фактическое количество с `total` и повторяет запрос с большим `take`, если не сошлось, вместо того чтобы тихо вернуть неполные данные. Поиск по коду ресурса через параметр `search=` не сработал при проверке (пустой результат) — используется полная выгрузка категории с локальным поиском по коду, не серверная фильтрация (не проблема после исправления пагинации, просто иначе устроено, чем ожидалось).

## Архитектура — 12 агентов

Не универсальный бот, а конвейер специализированных агентов. Для каждого агента должно быть явно зафиксировано: источник входных данных, формат выхода, критерий готовности, нужен ли человек-эксперт перед выдачей клиенту.

| № | Агент | Статус |
|---|---|---|
| 1 | Монитор (сбор данных ЕИС) | **С 2026-09-23 работает на реестре ИЗВЕЩЕНИЙ (`PRIZ`/`epNotificationEF2020`) — закупки, доступные для участия, не на реестре контрактов (архив уже заключённых сделок).** Продуктовое решение принято и реализовано: `EISConfig.from_env()` по умолчанию читает извещения, не контракты (см. «Известные пробелы» → «Решено»). Вся цепочка получения реальных данных (namespace конверта, токен на скачивание архива, аутентификация запроса) исправлена и подтверждена реальными прогонами Edwin, на обоих реестрах — из 1640 XML-документов реестра контрактов 674 нашли ОКПД2-код (8 стройка), из реестра извещений получен и разобран настоящий строительный документ (`0373200298826000007`) — оба подтверждения см. «Решено». **Не проверено на извещениях: находит ли `_find_okpd2_codes()` (классификатор Агента 1) в них ОКПД2 вообще** — структурный путь подтверждён только на контракте, см. открытый п.15; если не находит, монитор под новым источником может тихо вернуться к ложным нулям. `data/monitor.sqlite3` защищена от смешивания источников — даты, обработанные под старым (контракты), для нового (извещения) не считаются обработанными, `run_monitor.py` явно предупреждает, не молчит (см. «Решено»). Открытый FTP ЕИС закрыт с 01.01.2025 — не рабочий вариант. **Подключён к Агенту 2, 2026-09-21** (`tender_adapter.py`) — `notice_document_to_tender()` (извещение, путь по умолчанию) закрывает 7 из 10 полей `Tender` сам, `document_to_tender()` (контракты, оставлен в коде, не путь по умолчанию) — 2 из 10; оставшиеся `okpd2_code`/`region_code`/`publish_date` в извещении не искали, не подтверждены (см. п.13) — обязательные явные параметры, заглушка не подставляется |
| 2 | Классификатор (фильтр под клиента) | Реализован (`src/classifier`) — весь официальный раздел ОКПД2 «Строительство» (коды 41/42/43, 548 записей, источник — файл владельца `okpd.xlsx`). Решение владельца: без сужения, вся стройка в скоуп. Плюс сопоставление профиля клиента с конкретной закупкой (регион/СРО/опыт/финансы) на придуманных тестовых закупках — реальных данных из ЕИС для этого пока нет (см. п.1 про переходник от Агента 1 — он закрывает совместимость типов, но не сам источник остальных 8 полей `Tender`) |
| 3 | Аналитик документации | В работе — каркас (`src/document_analyst`): извлекает из текста документации закупки сроки подачи/исполнения, требования к обеспечению заявки/контракта, требования к участнику и отдельно помеченный список «скрытых рисков» (нестандартные штрафы, безлимитная неустойка, одностороннее право заказчика, расплывчатые формулировки). Извлечение — эвристика по ключевым словам/regex на трёх придуманных тестовых документах, метод для реальных документов не выбран (см. «Известные пробелы»). **Реально подключена та же логика аудита, что у Агента 4, 2026-09-21** — не только общее имя агента: `document_analyst.review_document_analysis()` (`src/document_analyst/review.py`) прогоняет решение эксперта через `AuditReadinessTracker` и `CategorizedDiscrepancyLog` (критическая/существенная/косметическая + что пропущено/неверно + комментарий эксперта), и реально вызывается из `src/review_queue.py` (рабочий CLI, не тест) при подтверждении/отклонении каждого пункта. Ограничение честно осталось прежним: трекер и лог создаются заново при каждом запуске CLI — окно из 50 проверок не копится между запусками, потому что у Агента 3 до сих пор нет постоянного хранилища результатов. **Технически подключён к Агенту 6** через `extracted_requirements` (`assemble_document_package(profile, tender, extracted_requirements=...)`) — обязательность полей обеспечения/допусков в пакете теперь учитывает и то, что извлёк Агент 3, не только структурированные поля `Tender`; скрытые риски прокидываются в `DocumentPackage.hidden_risks` для будущего Агента 8. Подключение требует `ExtractedRequirements.expert_reviewed=True` (жёсткая ошибка иначе) — проверка экспертом не автоматизирована |
| 4 | Сметчик (ФЕР/ТЕР/ГЭСН) | В работе — заложен фундамент (`src/smeta_estimator`), не полный агент. Источник — открытый bulk-доступ ФГИС ЦС без логина (см. «Известные пробелы», п.2, и `docs/agent4-ai-matching-feasibility.md`). Реализовано: (1) парсер ГЭСН/ФСБЦ (`fsnb_parser.py`) — 30 028 позиций работ в структурированный вид (шифр/название/единица/состав ресурсов), с расчётом `base_price` (базисный уровень цен 01.01.2022) по тем ресурсам, для которых нашлась прямая цена; (2) загрузка и разбор правильного источника региональных индексов — **ГОСР** (индексы по группам однородных строительных ресурсов, `regional_pricing_client.py`/`regional_pricing_parser.py`; после диагностики 2026-09-18 заменили изначально ошибочно использованный общий индекс «к ФЕР-2001/ТЕР-2001», решено — см. «Известные пробелы» → «Решено») плюс прямой запрос текущей цены ресурса **и** оплаты труда рабочих (`RimWorkerSalaryRegistry`, решено — см. там же), с проверенной и защищённой от тихой потери данных пагинацией (решено — см. там же); версия ГОСР-индексов на регион идёт через существующий протокол Агента 10 (`regulatory_updates.RegulatoryVersion`/`detect_update`/`approve_update`), новая структура не заводилась; (3) простой поиск кандидатов по ключевым словам (`search.py`, не embeddings — можно улучшить позже); (4) расчёт региональной цены **по каждому ресурсу отдельно**, с приоритетом (`pricing.py`): текущая цена/оплата труда напрямую → иначе базисная цена × ГОСР-индекс для группы именно этого ресурса → иначе не определена, честно, не ноль (что реально осталось неопределённым на тестовом примере — только категории материалов без выбранного продукта и один нерасшифрованный код ГЭСН, см. «Известные пробелы», п.10). Результат — всегда список кандидатов (`MatchResult`), не решение; выбор эксперта обязателен (`select_candidate`) и **впервые в проекте реально подключён** к общей инфраструктуре контроля качества (`matcher.review_match_result` → `quality_control.AuditReadinessTracker` + `CategorizedDiscrepancyLog`) — у агентов 3 и 10 на момент этой записи похожий протокол реализован каждый по-своему, не через один общий класс. Протестировано на реальных фрагментах ГЭСН/ФСБЦ/ГОСР/текущих цен/оплаты труда для Москвы (все вырезаны из настоящих ответов ФГИС ЦС, не выдуманы), плюс на тексте закупки «капремонт кровли школы №5» из тестовых данных проекта. **Подключён к Агенту 5** (`smeta_estimator/cost_estimate.py`, `build_cost_estimate()` → `SmetaCostResult.to_cost_estimate()`) — сводит подтверждённые экспертом позиции сметы в одну себестоимость (`CostEstimate`), см. п.12 «Известных пробелов» про объём работ. Оплата труда машиниста (`MachineLabourInfo`, `LabourMach`×ставка) реализована, но не финальна — основана на устном, не письменном подтверждении Smetrix, см. п.9. Не реализовано: содержательный расчёт сметы по объёмам работ (сами объёмы этот фундамент не считает, см. п.12), варианты ГЭСНм/мр/п/р, разрешение AbstractResource в конкретный продукт (см. п.10) |
| 5 | Оценка выгоды (маржа) / скоринг вероятности победы | В работе — каркас (`src/profitability_estimator`): считает маржу = НМЦК − Себестоимость − Стоимость_обеспечения − Налоги по паре клиент+закупка. Себестоимость принимается явным параметром (`CostEstimate`) — теперь реально строится Агентом 4 (`smeta_estimator.build_cost_estimate()` → `SmetaCostResult.to_cost_estimate()`), не переданным вручную придуманным числом; интерфейс `estimate_profitability()` при этом не изменился, как и было задумано, но сам `CostEstimate` — да: добавлены `expert_reviewed` (тот же гейт, что `ExtractedRequirements.expert_reviewed` у Агента 3 — `estimate_profitability()` отказывает без него) и `is_complete` (риск-флаг, не отказ, если часть позиций сметы не оценена). Единственное, что осталось не таким гладким, как ожидалось — объём работ по смете конкретной закупки Агент 4 не считает, это по-прежнему ручной вход (`SmetaLineItem.work_volume`, см. «Известные пробелы», п.12). Стоимость обеспечения — 3% годовых от суммы обеспечения контракта (из `SecurityRequirement` Агента 3, сумма при необходимости считается как %×НМЦК) за срок исполнения (`SubmissionTimeline`), 0 если банковской гарантии нет в профиле; если данных Агента 3 не хватает — явный флаг «не удалось посчитать», а не тихий ноль. Налоги — по ставке из выбора клиента (`TaxRegimeChoice`), таблица ставок не проверена бухгалтером (см. «Известные пробелы», п.7); режим «Другое» блокирует расчёт маржи явным флагом. Числовая вероятность победы **не считается** — вместо неё качественные флаги риска (строгость требований к участнику по количеству `ParticipantRequirement`, повышенный риск срыва исполнения при скрытых рисках категорий «нестандартный штраф»/«безлимитная неустойка») и обязательная пометка `WIN_PROBABILITY_NOTE` про переход на статистику через будущего Агента 9 (см. «Известные пробелы», п.8). Как и у Агента 6, требует `ExtractedRequirements.expert_reviewed=True`, если Агент 3 подключён. **Подключён к Агенту 8, 2026-09-21** (`build_client_summary(..., profitability=...)`) — раньше маржа и риск-флаги считались, но никуда дальше не попадали: до собственника в сводке не доходили вообще, хотя показать их клиенту и есть весь смысл этого агента |
| 6 | Сборщик документов | В работе — каркас (`src/document_assembler`): проверка статуса профиля READY (жёсткая ошибка, если не READY) + заготовка полей пакета с указанием источника каждого поля. Теперь принимает необязательный `extracted_requirements` от Агента 3 (сроки/обеспечение/допуски/скрытые риски) и учитывает его при определении обязательности полей — например, обеспечение исполнения контракта, которого вообще нет в `Tender`. Содержательная генерация документов по-прежнему не реализована. Метрика готовности к выборочному аудиту уже реализована в `src/quality_control` |
| 7 | Проверка комплектности | В работе — каркас (`src/completeness_check`): берёт заготовку пакета от агента 6 и данные закупки, сверяет, что пакет относится к этой закупке, и выносит вердикт PASS/FAIL по обязательным полям с конкретным списком недостающих (плюс отдельный информационный список недостающих необязательных). Работает на признаке `required` в `PackageField` (агент 6 теперь его сохраняет, а не отбрасывает) |
| 8 | Консультант для клиента | В работе — каркас (`src/client_consultant`): берёт результат всей цепочки — матчинг закупки от агента 2, вердикт комплектности от агента 7, пакет документов от агента 6 (в нём же — скрытые риски агента 3) — и собирает сводку для собственника простыми словами: подходит ли закупка, готовы ли документы, что конкретно не хватает и что с этим делать, каждый скрытый риск — с переводом «что это значит на практике». Ничего не рекомендует и не решает, явное напоминание об этом — в каждой сводке. Защиты от невыверенных экспертом данных Агента 3 отдельно не делали — она уже наследуется от Агента 6 (`assemble_document_package` не даст собрать пакет со скрытыми рисками без `expert_reviewed=True`). **Принимает и результат Агента 5, 2026-09-21** (`profitability: ProfitabilityEstimate | None` — необязательный четвёртый вход, как `extracted_requirements` у Агента 6): если передан, сверяется на закупку/клиента и попадает в сводку отдельным пунктом «Ожидаемая выгода» (маржа простыми словами + риск-флаги + пометка про вероятность победы) — своя `ProfitabilitySummary`, не прямой проброс модели Агента 5, по тому же принципу, что `PlainRisk` для Агента 3. До этой правки ни поля в `ClientSummary`, ни параметра в `build_client_summary()` не было вообще — маржа считалась Агентом 5 и никуда дальше не попадала |
| 9 | Клиентская аналитика | Не специфицирован |
| 10 | Парсер нормативки | В работе — каркас (`src/regulatory_updates`): отслеживает два типа источников — базу расценок (ФЕР/ТЕР/ГЭСН, региональные индексы) и законодательство (44-ФЗ/223-ФЗ). Хранит текущую применённую версию каждого источника в SQLite (`RegulatoryUpdateStore`, по аналогии с `MonitorStore` у Агента 1) и при обнаружении новой версии строит явный diff по пунктам (старое значение → новое). Новая версия никогда не применяется автоматически — `detect_update()` только создаёт запись со статусом `PENDING`; применяет её `apply_update()`, который сам отказывается работать, пока запись не переведена в `APPROVED` через `approve_update(reviewer, approved=True/False)`. Отклонение обязательно требует причины, которая фиксируется в записи (`decision_reason`) и доступна через `list_rejected()` для анализа, по аналогии с логом расхождений Агента 4. Тестовые версии источников — придуманные, но реалистичные (`sample_versions.py`), реального источника пока нет (см. п.2 «Известных пробелов»). Интерфейс для решения эксперта — `src/review_queue.py` (см. ниже) |
| 11 | Онбординг клиента (профиль) | Реализован в коде (`src/onboarding`) — 4 блока полей (юр.данные, допуски/опыт, мощности, финансовая готовность) + статусная модель `черновик → заполнен → проверен экспертом → готов`, переход на последние две стадии — ручное действие человека, не автоматика. Блок финансовой готовности: налоговый режим — не свободный текст, а один выбор из 8 фиксированных комбинаций режим+ставка (`onboarding.tax_config.TaxRegimeChoice`), которую указывает сам клиент; среднегодовая выручка осталась отдельным полем для совсем другой цели (финансовая состоятельность у Агента 2), с налогами не связана |
| 12 | Юрист по договору с клиентом сервиса (границы ответственности) | Реализован каркас (`src/legal_boundaries`) — фиксированный текст `CLIENT_SUMMARY_LEGAL_NOTICE` (сервис не гарантирует победу, решение всегда за собственником, ответственность за устаревшие неподтверждённые данные профиля — не на сервисе), безусловно вшитый в конец `render_summary_text()` Агента 8 как неотъемлемая часть функции, а не опциональный параметр. Отдельно — черновик более формальной формулировки для будущего договора-оферты (`docs/legal-boundary-draft.md`). Оба текста не проверены юристом (см. «Известные пробелы», п.6) |

## Интерфейс эксперта-проверяющего

- `src/review_queue.py` — интерактивный CLI (не сайт): показывает всё, что ждёт решения Edwin, и даёт ответить да/нет прямо в терминале, не трогая код.
  - Реальная очередь: все записи `PENDING` у Агента 10 (`RegulatoryUpdateStore.list_pending()`) — с diff по пунктам и настоящим `approve_update()`/отклонением с причиной.
  - Учебный пример по источнику документов, но не по обработке решения: три тестовых документа Агента 3 (`sample_documents.py`), прогнанные через `extract_requirements()` при каждом запуске. Само решение эксперта, однако, реально прогоняется через `document_analyst.review_document_analysis()` (`AuditReadinessTracker`/`CategorizedDiscrepancyLog`, 2026-09-21) — не только помечает объект в памяти. Не сохраняется только сама метрика между запусками CLI (у Агента 3 нет постоянного хранилища результатов, см. «Известные пробелы»).
- **Заметка на будущее, не сейчас:** терминал годится, пока проверяющий — один человек (Edwin) и очередь короткая. Если объём вырастет или проверяющих станет больше, для реального интерфейса может понадобиться что-то удобнее — веб-страница в самом продукте или общая Google-таблица с формулами/фильтрами. Явного решения пока нет, `review_queue.py` — временное практичное решение под текущий масштаб пилота.

## Протокол контроля качества — обязательные правила, без исключений

- Выдача агентов 3, 4, 6 (документация, смета, пакет документов) не уходит клиенту без подтверждения эксперта-человека, пока не накоплена статистика и не согласован переход на выборочный аудит.
- Профиль клиента (агент 11) проверяется на полноту и достоверность до того, как им пользуется агент 6.
- Любое обновление нормативной базы (агент 10) применяется только после подтверждения эксперта — автоматическое применение запрещено.
- **Автоматическая подача заявки и автоматическая подпись КЭП без участия собственника — исключены архитектурно.** Не реализовывать эту функциональность ни в каком виде, даже как опцию.
- Если агент выдаёт смету/документацию — обязательно указывать дату актуальности нормативных данных, на которые опирается расчёт.

## Технические решения, уже принятые

- Продукт: сайт (веб-приложение) + Telegram-бот. Нативные iOS/Android — не на старте.
- Источник данных ЕИС: официальный доступ через zakupki.gov.ru, регистрация как «физическое лицо, индивидуальный предприниматель» → токен (не mTLS-сертификат, это только для юрлиц). Переменная `EIS_CONSUMER_TYPE=individual_person`. Не FTP, не HTML-скрапинг — ненадёжно и неполно по НМЦК; открытый FTP ЕИС закрыт с 01.01.2025.
- Метрика готовности агента к переходу с полной проверки эксперта на выборочный аудит (применяется к агентам 3, 4, 6): скользящее окно 50 проверок, ≥90% без существенной корректировки, 0 критических ошибок, откат при любой критической ошибке после перехода.
- Хостинг для постоянно работающих агентов (монитор и далее) — Railway, отдельный новый проект на существующем аккаунте Edwin, не смешивать с другими его проектами.
- Агент 10 (Парсер нормативки, `src/regulatory_updates`): подтверждение экспертом обязательно для абсолютно каждой версии источника, включая самую первую («нулевую», когда предшественника ещё нет) — осознанное решение владельца, не временная заглушка. **Заметка на будущее, не сейчас:** если на практике подтверждать по одной 10-15 региональных версий вручную окажется неудобно, можно добавить пакетное подтверждение (одобрить сразу несколько `PENDING`-записей одним действием) — это только ускорение самого процесса подтверждения, саму обязательность экспертной проверки такая доработка не отменяет.
- Налоговый режим в профиле клиента (Агент 11, `onboarding.tax_config`): вместо свободного текста «система налогообложения» + отдельного дохода, из которых сервису пришлось бы самому вычислять ставку НДС и следить за порогами/индексацией УСН — клиент выбирает готовую комбинацию режим+ставка сам, из ровно 8 фиксированных вариантов (`TaxRegimeChoice`: УСН 6%/15% без НДС или с НДС 5%/7%, ОСН+НДС 22%, либо «другое — уточнить с бухгалтером»). Агент 5 (когда будет спроектирован) берёт ставку прямо из этого выбора, не вычисляет её. `tax_config.py` — справочник для валидации выбора в форме, не калькулятор; там же `UNVERIFIED_NOTES` с непроверенной информацией о возможном возврате ставки НДС к 20% с 2027 года — заметка на будущее, автоматически ничего не меняет. Среднегодовая выручка (`avg_annual_revenue`) осталась отдельным полем для другой цели — финансовой состоятельности клиента у Агента 2, с налоговым режимом не связана.

## Стиль работы

- Объяснять технические шаги простыми словами — владелец не разработчик.
- Не делать необратимых или дорогостоящих шагов (деплой в прод, трата денег на подписки/API) без явного подтверждения.
- Каждое техническое решение сверять с протоколом контроля качества выше.
- Коммитить и пушить в GitHub после каждого законченного куска работы, не ждать полной готовности всего агента.
