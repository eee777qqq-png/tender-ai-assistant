__pycache__/
*.pyc
.venv/
venv/
.env
certs/
*.pem
*.key
*.pfx
.pytest_cache/
data/monitor.sqlite3
data/regulatory_updates.sqlite3
# Сырые архивы ЕИС для ручного разбора (fetch_purchases.py --save-raw) — не коммитить
data/raw/
data/raw_notices/
data/raw_notices*/

# Реальный профиль клиента (Агент 11) — настоящие ПДн (ИНН, адрес, телефон,
# email), не тестовые данные. См. src/real_client_profile.py.example —
# шаблон без реальных значений, тот коммитится.
src/real_client_profile.py
